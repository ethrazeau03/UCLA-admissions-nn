# UCLA Admission Prediction using Neural Network

## Purpose
This project modularizes a UCLA notebook into a maintainable ML application using `MLPClassifier`.

## Expected dataset
Place your dataset in:
`data/raw/Admission.csv`
